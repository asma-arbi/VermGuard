/**
 * Rôles des utilisateurs dans VermGuard.
 * - manager : Responsable/Manager du SOC ou Support.
 * - soc : Analyste SOC.
 * - support : Agent du Support.
 */
export enum UserRole {
  MANAGER = 'manager',
  SOC = 'soc',
  SUPPORT = 'support',
}
